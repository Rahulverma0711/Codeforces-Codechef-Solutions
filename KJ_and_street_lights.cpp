#include <bits/stdc++.h>
using namespace std;
int main()
{
ios_base :: sync_with_stdio(false);
cin.tie(nullptr);
cout.tie(nullptr);
int n,p;
cin>>n>>p;
for(int i=0;i<n;i++)
{
    cin>>x>>r;
    c[max(0,x-r)]+=1;
    c[max(x+r,p)+1]-=1;
}
for(int i=0;i<)
return 0;
}