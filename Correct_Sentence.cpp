#include <bits/stdc++.h>
using namespace std;
int main()
{
ios_base :: sync_with_stdio(false);
cin.tie(nullptr);
cout.tie(nullptr);
int t;
cin>>t;
while(t--)
{
    int n;bool check1=false;
    while(n--)
    {
        string s;
        cin>>s;
        bool check=false;
        for(int i=0;i<s.size();i++)
        {
           
        }
        if(check)
        check1=true;
    }
    if(check1)
    cout<<"YES"<<"\n";
    else 
    cout<<"NO"<<"\n";
}
return 0;
}